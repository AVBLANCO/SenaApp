<?xml version="1.0" encoding="utf-8"?>
<tabs>

    <tab
        id="@+id/find"
        title="@string/tab_find"
        barColorwhenSelected="@color/primaryColor"
        icon="@drawable/ic_find"/>

    <tab
        id="@+id/home"
        title="@string/tab_home"
        barColorwhenSelected="@color/primaryColor"
        icon="@drawable/ic_home"/>

    <tab
        id="@+id/profile"
        title="@string/tab_profile"
        barColorwhenSelected="@color/primaryColor"
        icon="@drawable/ic_profile"/>
</tabs>